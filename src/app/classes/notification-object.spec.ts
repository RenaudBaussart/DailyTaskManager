import { NotificationObject } from './notification-object';

describe('NotificationObject', () => {
  it('should create an instance', () => {
    expect(new NotificationObject()).toBeTruthy();
  });
});
